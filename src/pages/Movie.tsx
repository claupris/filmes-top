import { Link } from "react-router-dom";

const Movie = () => {
    return (
        <div>
        <p>Movie</p>

        <Link to="/category">category</Link>
        </div>
    );
}

export default Movie;